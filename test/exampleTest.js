describe('queryManager', () => {
  let queryManager;

  beforeEach(function() {
    queryManager = require('@app/helpers/queryManager');
  });

  it('getBaseQuery', () => {
    let arr = [];
    assert.equal(arr.length, 0);
  });
});


